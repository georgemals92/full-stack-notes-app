# Example environment variables for full-stack-notes-app
# Copy this file to .env and fill in real values (do NOT commit .env with secrets).

# MongoDB connection string. Example for local MongoDB:
# MONGO_URI=mongodb://localhost:27017/notesdb
MONGO_URI=mongodb://127.0.0.1:27017/full-stack-notes-app

# Server port (optional). Default in server code is 4000 when not set.
PORT=4000

# Optional: NODE_ENV (development|production)
# NODE_ENV=development

# Notes:
# - After creating .env from this example, restart the server so dotenv picks up values.
# - Keep real secrets out of source control. Use environment-specific secret management in production.
